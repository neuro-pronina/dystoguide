version: "3.8"
services:
  app:
    build: .
    ports:
      - "8501:8501"
    environment:
      - OPENFACE_CMD=/opt/OpenFace/build/bin/FeatureExtraction
    volumes:
      - ./data:/app/data
    command: ["streamlit", "run", "app.py", "--server.port=8501", "--server.address=0.0.0.0"]
